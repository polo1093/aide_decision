# Agent Instructions

- When fixing a bug, add a focused regression test by default.
- If a regression test is not practical, state the reason clearly in the handoff.
- Run the targeted test file after the fix, and run broader tests when shared behavior changed.
